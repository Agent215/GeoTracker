/**
 * 
 * This is a baseline function to make sure our test suite is working properly.
 * If this test fails somthing is wrong with jest testing configuration.
 */

it('works', () => {
    expect(1).toBe(1);
  });